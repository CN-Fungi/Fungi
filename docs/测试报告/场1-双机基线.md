# 场① 双机基线（约 90 分钟）

- 操作：**人1 起房、人2 加入**
- 记录员：**人1**（这份由人1 填，改完提 PR）
- 机器：人1 `<机器名/IP>`、人2 `<机器名/IP>`；同网段 `<192.168.x.0/24>`
- 版本：Fungi v0.6.1 打包版
- 日期：2026-09-__　时间：__:__ – __:__
- 房间 token：`fungi-test`

## 开跑前 5 分钟（固定动作）

- [ ] 两台机都**清空 `data/`**
- [ ] 两台机都确认 key 已配好、能单独对话
- [ ] 确认在同一网段、都没开代理/防火墙拦着
- [ ] 定好谁起房、谁加入

## 登记表

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| X1 | 自动发现与端口 | 待测 | |
| X2 | 名册互见与心跳 | 待测 | |
| X3 | 留言与已读 | 待测 | |
| X4 | 信使转告流 | 待测 | |

---

### X1 自动发现与端口（05–20 min）

- **步骤**：① 人1 填 token `fungi-test` 发起房间 → ② 人2 **只填 token、不填 IP** 点加入（同网段自动发现）→ ③ 两台机各起一个房间，看端口
- **期望**：人2 能加入；第二个房间的端口会从 `8899` **向上找一个空闲的**（不是报"端口被占用"）
- **证据**：`netstat -ano | findstr 889` 两台机的截图 + 加入过程的录屏
- **实际**：
- **判定**：待测
- **Issue**：

### X2 名册互见与心跳（20–40 min）

- **步骤**：① 两端都看到对方在线（截图）→ ② 把人2 的网断 30 秒再恢复 → ③ 看名册什么时候变化、什么时候恢复
- **期望**：变化与恢复都有据可查；不会永久挂着一个掉线的名字
- **证据**：录屏 + 两端名册截图
- **实际**：
- **判定**：待测
- **Issue**：

### X3 留言与已读（40–60 min）

- **步骤**：① 人1 给人2 留一条言 → ② 人2 在好友视图看它是不是**气泡**内联渲染 → ③ 点开视为已读 → ④ 两端各看 `data/mail/<主机>.jsonl`
- **期望**：留言能送达、渲染成气泡、点开即已读；**两端各有一份** `data/mail/` 记录
- **证据**：给留言与已读的截图 + 两端的 `data/mail/` 文件列表与内容片段
- **实际**：
- **判定**：待测
- **Issue**：

### X4 信使转告流（60–80 min）

- **步骤**：① 人2 **关掉应用离开** → ② 人1 问一件需要人2 回答的事（例如"问一下他明天有没有空"）→ ③ 人2 回来后看自己的信使怎么转告 → ④ 核 `data/comm-sessions/`
- **期望**：人2 不在时由他的信使代答（重要消息转告、寻常消息代答）；人2 回来后能看到这条往来
- **证据**：两端录屏 + `data/comm-sessions/` 截图
- **实际**：
- **判定**：待测
- **Issue**：
- **备注**：信使**不会自动回信**，它不理你是设计

## 收尾（80–90 min）

- [ ] 失败项**当场**开 Issue，并填进上面登记表的 Issue 列
- [ ] 各存一份 `data/` 目录留档
- [ ] 记录本轮总耗时与卡住的地方
