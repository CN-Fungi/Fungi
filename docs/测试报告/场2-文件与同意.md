# 场② 文件与同意（约 90 分钟）

- 操作：**人2 起房、人3 加入**
- 记录员：**人2**（这份由人2 填，改完提 PR）
- 机器：人2 `<机器名/IP>`、人3 `<机器名/IP>`；同网段 `<192.168.x.0/24>`
- 版本：Fungi v0.6.1 打包版
- 日期：2026-09-__　时间：__:__ – __:__
- 房间 token：`fungi-test-2`

> 换配对是有意的：让每个人都当过一次操作者、也各自记录过一场。

## 开跑前 5 分钟（固定动作）

- [ ] 两台机都**清空 `data/`**
- [ ] 两台机都确认 key 已配好
- [ ] 找一个 20MB 左右的测试文件，两台机各放一份
- [ ] 记下跑之前 `inbox/` 与 `data/transfers/` 的样子（截图，后面好对比）

## 登记表

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| X5 | 文件双向传输 | 待测 | |
| X6 | 同意卡片三态 | 待测 | |
| X7 | 权限滑块 | 待测 | |
| X8 | 信使开关即时生效 | 待测 | |

---

### X5 文件双向传输（05–30 min）

- **步骤**：① 人2 给人3 发一个 20MB 文件，人3 同意收下 → ② 反过来人3 给人2 发一个 → ③ 看进度模态框（传完是否自动关闭）→ ④ 看落盘位置 `inbox/<来源主机>/` → ⑤ 看 `data/transfers/` 取走后是否**清空**
- **期望**：两个方向都能传；进度框传完自动关；文件落在 `inbox/<来源主机>/`；`transfers/` 只是中转、取走即删
- **证据**：录屏 + 两端 `inbox/` 与 `data/transfers/` 截图
- **实际**：
- **判定**：待测
- **Issue**：

### X6 同意卡片三态（30–60 min）

- **步骤**：让对方**跨主机**做一次需要许可的文件操作，依次试三种答复：① 允许 → ② 禁止 → ③ 自定义输入
- **期望**：三种都能收到；答复原路唤醒请求方；「禁止」时请求方得到明确拒绝而不是卡住
- **证据**：三次的录屏
- **实际**：
- **判定**：待测
- **Issue**：

### X7 权限滑块（60–75 min）

- **步骤**：① 好友视图顶部滑块拨到「允许」→ 再让对方发一个文件 → ② 拨回「询问」→ 再让对方发一次 → ③ 看 `~/.fungi/consent_rules.json`
- **期望**：「允许」时**不再弹卡**直接放行；「询问」时重新弹卡；规则真落盘
- **证据**：滑块与弹卡有无的对照录屏 + json 截图
- **实际**：
- **判定**：待测
- **Issue**：

### X8 信使开关即时生效（75–90 min）

- **步骤**：① 设置页**关掉**本机信使 → ② 让对方给你留言、并发一个文件 → ③ **不重启**再打开信使 → ④ 再让对方留言一次
- **期望**：关掉后留言**直达**会话视图、同意卡由 hub 直推、本机 Agent 零消耗；打开后恢复代收代复，**不重启即生效**
- **证据**：录屏 + 设置页开关截图
- **实际**：
- **判定**：待测
- **Issue**：

## 收尾（90+ min）

- [ ] 失败项**当场**开 Issue，填进登记表
- [ ] 各存一份 `data/` 与 `inbox/` 留档
- [ ] 记录总耗时与卡住的地方
