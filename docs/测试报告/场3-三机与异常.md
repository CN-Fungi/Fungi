# 场③ 三机与异常（约 100 分钟）

- 操作：**三人全上**——一台 server、两台 client
- 记录员：**人3**（这份由人3 填，改完提 PR）
- 机器：server `<机器名/IP>`、client A `<机器名/IP>`、client B `<机器名/IP>`；同网段 `<192.168.x.0/24>`
- 版本：Fungi v0.6.1 打包版
- 日期：2026-09-__　时间：__:__ – __:__
- 房间 token：`fungi-test-3`

> 这一场是**唯一实测服务器转发**的机会：一台机开两个端口模拟不算。缺机器时用 Blinvo 的机器补 server；**只有两台机器时，X9 写「不可测：缺第三台主机」**，别编一个通过。

## 开跑前 10 分钟（固定动作）

- [ ] 三台机都**清空 `data/`**
- [ ] 三台机都确认 key 已配好
- [ ] 定好谁是 server（server 那台的 `data/` 会额外承载共享文件空间）
- [ ] 三台机都进同一个房间，名册上互相看得见（截图）

## 登记表

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| X9 | 三机转发与投递质量 | 待测 | |
| X10 | 断线重连与卡片重放 | 待测 | |
| X11 | 防火墙未放行 → 自动 UAC | 待测 | |

---

### X9 三机转发与投递质量（10–40 min）

- **步骤**：① client A 发消息给 client B（必须经过 server 中转）→ ② **至少连发 30 条**带序号的短消息 → ③ 核对 B 收到了哪些、有没有重复、顺序有没有乱 → ④ 看 server 上的 `data/comm/`
- **期望**：30 条都到；重复投递由消息 id 去重（同一条不会出现两次）；server 的 `data/comm/` 里每条投递都有一条信封镜像
- **证据**：A 与 B 的消息列表截图（能数得清）+ server `data/comm/` 截图 + 录屏
- **实际**：
- **判定**：待测
- **Issue**：

### X10 断线重连与卡片重放（40–70 min）

- **步骤**：① 制造一张**未决**的同意卡（B 请求、A 别点）→ ② 把 B 的网络断掉 → ③ 等 30 秒恢复 → ④ 看那张卡是否自动重放、请求方是否被唤醒 → ⑤ 再测一次应用重启（关掉 client B 的主界面/进程再开）
- **期望**：未决卡片断线重连后**自动重放**，不丢；答复原路唤醒发起方
- **证据**：**必须录屏**全程
- **实际**：
- **判定**：待测
- **Issue**：

### X11 防火墙未放行 → 自动 UAC（70–85 min）

- **前置**：一台**从未放行过** `Fungi.exe` 的机器（打包版；源码版早年放行过 `python.exe`，不能用它验）
- **步骤**：① 那台机进房间 → ② 手机连同一网段扫码 → ③ 看手机端页那一行状态 → ④ 点它发起的 UAC，允许 → ⑤ 再取消一次，看是否留一个手动重试按钮
- **期望**：状态**一直显示**（放行了就写「已放行 <程序名>」，没放行就写原因 + 按钮）；没放行时自己发起一次 UAC；取消后留按钮可手动重试
- **证据**：**那一行的截图**（放行与未放行两种）+ 手机页面截图
- **实际**：
- **判定**：待测
- **Issue**：

## 收尾（85–100 min）

- [ ] 三人一起过一遍失败项，**当场**开 Issue 并填登记表
- [ ] 各存一份 `data/` 留档（server 那份尤其重要）
- [ ] 记录总耗时、三机的网段与 IP 组合
