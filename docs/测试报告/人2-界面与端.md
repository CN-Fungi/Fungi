# 人2 · 界面与端（单机，不需要第二台机器）

- 执行人：<你的 GitHub 用户名>
- 机器：<机器名> / 网段 <192.168.x.x>
- 版本：Fungi v0.6.1 打包版；解压路径 <纯英文路径>
- 日期：2026-09-__
- 本份文件同时是 **场②（文件与同意）的记录员** 用表，见 `场2-文件与同意.md`

界面名称对不上时，以应用左侧栏为准：**发起房间 / 加入房间 / 手机端 / 信使 / 设置 / 帮助**。

## 登记表（结果填这里，逐条改成 通过 / 失败 / 不可测）

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| 2-01 | 自测钩子（**源码方式**） | 待测 | |
| 2-02 | 启动器六个页面 | 待测 | |
| 2-03 | 关窗转托盘与托盘菜单 | 待测 | |
| 2-04 | 单实例 | 待测 | |
| 2-05 | PC WebUI 基础（会话 / 渲染 / 一键重试） | 待测 | |
| 2-06 | 桌面端文件发送进度 | 待测 | |
| 2-07 | 移动端扫码 / 抽屉 / 上传 | 待测 | |
| 2-08 | 手机给对方发文件（两条进度） | 待测 | |
| 2-09 | 模型配置两个入口 + 错 key | 待测 | |
| 2-10 | 铃声与音色 | 待测 | |
| 2-11 | PC 与手机一致性 / 提示不泄露 | 待测 | |

---

### 2-01 自测钩子（**源码方式**，打包版没有 stdout）

- **前置**：源码方式，装有依赖
- **步骤**：`FUNGI_SELFTEST=1 python -m fungi --server --token x --data %TEMP%\fungi-selftest`
- **期望**：约 7 秒出现 `FUNGI SELFTEST OK`（托盘 + 卡片应答 + 阻塞解除全链路）
- **实际**：
- **判定**：待测
- **证据**：终端截图

### 2-02 启动器六个页面

- **前置**：无
- **步骤**：`发起房间` / `加入房间` / `手机端` / `信使` / `设置` / `帮助` 逐个打开，每页把能点的按钮都点一遍
- **期望**：都有内容、没死按钮、没空页面、没错别字；「打开 WebUI」真能打开浏览器
- **实际**：
- **判定**：待测
- **证据**：每页一张截图

### 2-03 关窗转托盘与托盘菜单

- **前置**：已经在房间里
- **步骤**：① 点主窗口关闭按钮 → ② 让另一个人给你发条消息，看还收不收得到 → ③ 单击托盘图标 → ④ 右键托盘 →「显示主界面」→ ⑤ 右键 →「退出」
- **期望**：关窗**不退出应用**、房间继续跑；单击托盘直接进 WebUI 好友视图；「退出」才真退（任务管理器里进程消失）
- **实际**：
- **判定**：待测
- **证据**：录屏 + 托盘菜单截图

### 2-04 单实例

- **前置**：应用已经开着
- **步骤**：再双击一次 `Fungi.exe`
- **期望**：唤起已运行的主界面，**不出现第二个进程**
- **实际**：
- **判定**：待测
- **证据**：任务管理器截图

### 2-05 PC WebUI 基础

- **前置**：已进房间
- **步骤**：① 新建会话、切换会话 → ② 发一条消息看渲染 → ③ 输入框**留空**点发送（一键重试）→ ④ 刷新页面
- **期望**：都正常；重试**不产生重复消息**；刷新后历史还在
- **实际**：
- **判定**：待测
- **证据**：截图

### 2-06 桌面端文件发送进度

- **前置**：有另一个人在线
- **步骤**：① 发一个约 20MB 的文件给对方 → ② 再发一次，让对方**拒绝**
- **期望**：出现进度模态框（风格与其他确认框一致）、传完**自动关闭**；被拒时写明原因并**留在屏幕上**
- **实际**：
- **判定**：待测
- **证据**：录屏

### 2-07 移动端扫码 / 抽屉 / 上传

- **前置**：手机与电脑同一网段
- **步骤**：① 电脑打开 `手机端` 页 → ② 手机扫码（**不需要 token**）→ ③ 右划任意位置开抽屉 → ④ 从手机上传一个文件到电脑
- **期望**：全部可用；上传的文件真出现在电脑上
- **实际**：
- **判定**：待测
- **证据**：手机与电脑同屏截图 + 录屏

### 2-08 手机给对方发文件（两条进度）

- **前置**：对方在线
- **步骤**：从手机给好友发一个文件
- **期望**：**两条进度**分别说明——先传到电脑、再由电脑送给对方；哪一跳卡住能看出来
- **实际**：
- **判定**：待测
- **证据**：录屏

### 2-09 模型配置两个入口 + 错 key

- **前置**：可临时把 `config.json` 备份后清空
- **步骤**：① 清掉配置启动，看网页端是否提示未配置 → ② 在启动器设置页重新填好 → ③ 故意填一个错 key 试一次对话
- **期望**：两个入口都能配；错 key **报错可读**，不是静默失败
- **实际**：
- **判定**：待测
- **证据**：截图

### 2-10 铃声与音色

- **前置**：无
- **步骤**：① 设置页打开来信提醒、7 个音色逐个**试听** → ② 换一个音色 → ③ 让另一个人给你发条留言 → ④ 重启应用
- **期望**：来信响**一次**；重启后音色设置保留
- **实际**：
- **判定**：待测
- **证据**：设置页截图

### 2-11 PC 与手机一致性 / 提示不泄露

- **前置**：两端都开着
- **步骤**：① 同一个会话在 PC 与手机各看一遍 → ② 逐个触发错误提示（错 token、错 key、发文件被拒）
- **期望**：功能一致、UI 略有不同；错误提示里**不出现本机绝对路径、不出现 token**
- **实际**：
- **判定**：待测
- **证据**：截图
