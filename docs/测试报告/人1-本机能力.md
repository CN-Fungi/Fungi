# 人1 · 本机 Agent 能力（单机，不需要第二台机器）

- 执行人：<你的 GitHub 用户名>
- 机器：<机器名> / 网段 <192.168.x.x>
- 版本：Fungi v0.6.1 打包版；解压路径 <纯英文路径>
- 日期：2026-09-__
- 本份文件同时是 **场①（双机基线）的记录员** 用表，见 `场1-双机基线.md`

## 登记表（结果填这里，逐条改成 通过 / 失败 / 不可测）

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| 1-01 | 文件读写三件套 | 待测 | |
| 1-02 | `edit` 精确替换 | 待测 | |
| 1-03 | bash 与超时 | 待测 | |
| 1-04 | 后台命令 | 待测 | |
| 1-05 | 交互式 REPL | 待测 | |
| 1-06 | 子代理 | 待测 | |
| 1-07 | 主动询问 | 待测 | |
| 1-08 | 技能沉淀 → 新会话生效 | 待测 | |
| 1-09 | 日记守口 | 待测 | |
| 1-10 | 待办与日历 | 待测 | |
| 1-11 | 桌面控制①：点开微信发消息 | 待测 | |
| 1-12 | 桌面控制②：新建文本文档并输入 | 待测 | |
| 1-13 | 网页抓取与搜索 | 待测 | |
| 1-14 | 视频理解（**源码方式**，最后再看） | 待测 | |

---

### 1-01 文件读写三件套

- **前置**：准备一张 png、一个 .docx、一个 .txt，放在同一个目录
- **步骤**：① 对它说「读一下这三个文件，说出内容」→ ② 再说「写一个能跑的贪吃蛇 HTML 到那个目录」→ ③ 打开写出来的文件看一眼
- **期望**：三种文件都能读出内容（图片走视觉通道）；HTML 真落盘且双击能打开
- **实际**：
- **判定**：待测
- **证据**：录屏 + 目录截图

### 1-02 `edit` 精确替换

- **前置**：准备一个 .txt，里面有一处**唯一**的句子和一处**重复两次**的句子
- **步骤**：① 让它改唯一那处 → ② 再让它改重复那处
- **期望**：第一次成功；第二次**报错并说明不唯一**，而不是随手改一处
- **实际**：
- **判定**：待测
- **证据**：改前改后文件内容截图

### 1-03 bash 与超时

- **前置**：无
- **步骤**：① 让它跑 `dir` → ② 让它跑一条要 10 分钟的命令
- **期望**：前者回结果；后者 600 秒硬超时被掐断，回合**不永久挂住**
- **实际**：
- **判定**：待测
- **证据**：录屏

### 1-04 后台命令

- **前置**：无
- **步骤**：① 让它把一条 60 秒的命令丢后台 → ② 看它是否立刻回话、能不能继续聊别的 → ③ 命令结束后看【后台报告】与右上角气泡 → ④ 点气泡里的停止
- **期望**：丢后台后立刻能继续对话；完成后以【后台报告】进入新一轮；气泡可点开、可停止
- **实际**：
- **判定**：待测
- **证据**：录屏

### 1-05 交互式 REPL

- **前置**：无
- **步骤**：① 让它起一个交互式 python → ② 喂 `print(1+1)` → ③ 再喂一行别的 → ④ 让它结束掉
- **期望**：逐轮读→写往返，回合不挂住；结束后任务管理器里**没有残留 python 进程**
- **实际**：
- **判定**：待测
- **证据**：录屏 + 任务管理器截图

### 1-06 子代理

- **前置**：无
- **步骤**：① 让它派一个子 Agent 去统计某个目录的文件数
- **期望**：立即返回 dispatched；报告在**后一轮**注入，不占当前回合
- **实际**：
- **判定**：待测
- **证据**：录屏

### 1-07 主动询问

- **前置**：无
- **步骤**：① 让它问你一个问题 → ② 再让它一次问一串（表格式多问）→ ③ 其中一次**故意不回答**
- **期望**：卡片问答正常；你不答也**不把它卡死**，答复晚些仍能作为新一轮回到它手里
- **实际**：
- **判定**：待测
- **证据**：截图

### 1-08 技能沉淀 → 新会话生效

- **前置**：无
- **步骤**：① 让它做一件会踩坑的事 → ② 说「把刚才的完整过程整理成技能」→ ③ **新建会话**，做同类事情 → ④ 看 `data/skills/<名字>/SKILL.md`
- **期望**：SKILL.md 落地；新会话**真的调用**这个技能
- **实际**：
- **判定**：待测
- **证据**：skill 文件截图 + 新会话录屏

### 1-09 日记守口

- **前置**：设置页「实验性」打开日记开关
- **步骤**：① 聊一次有触动的话题 → ② 直接问它日记写了什么 → ③ 自己去 `data/diary/` 看有没有当天一页
- **期望**：它守口如瓶；文件里确实有当天记录
- **实际**：
- **判定**：待测
- **证据**：它回答的截图 + `data/diary/` 文件列表截图
- **注意**：这是设计不是 bug；**不要评价它写了什么，更不要往外传**

### 1-10 待办与日历

- **前置**：无
- **步骤**：① 让它 add 两条不同日期的待办 → ② 让它 update 一条 → ③ 让它 remove 一条 → ④ 你在信使页手写一条，再让它改你这条
- **期望**：增删改都对；它**改不动用户手写的条目**
- **实际**：
- **判定**：待测
- **证据**：`data/todos.json` 内容截图

### 1-11 桌面控制①：点开微信发消息

- **前置**：设置页「实验性」打开「桌面控制」（默认关）；微信已登录——**用小号**，别拿真号
- **步骤**：① 说「打开微信，给<某人>发一句测试消息」→ ② 全程看它怎么动
- **期望**：自己把窗口叫到前台、点中输入框、**逐字落字**（约 6~7 字/秒，看得见节奏，不是"啪"一下糊上去）
- **实际**：
- **判定**：待测
- **证据**：**录屏必须**
- **注意**：自绘输入框里的逐字输入是已知**还没复测**的点，失败就如实报

### 1-12 桌面控制②：新建文本文档并输入

- **前置**：同 1-11
- **步骤**：① 说「在桌面新建一个文本文档，输入一段话」→ ② 打开那个文档核对内容
- **期望**：桌面上出现新文档、内容与要求一致；每步做完它自己会看一眼校验
- **实际**：
- **判定**：待测
- **证据**：录屏 + 文档打开截图

### 1-13 网页抓取与搜索

- **前置**：无
- **步骤**：① 给它一个网址让它说正文 → ② 让它搜一个话题
- **期望**：正文与网页对得上；搜索返回标题 / URL / 摘要
- **实际**：
- **判定**：待测
- **证据**：截图

### 1-14 视频理解（**源码方式**，放在最后）

- **前置**：源码方式跑（打包版没有 Python 解释器，跑不了）；首次要下模型，很慢
- **步骤**：① 给一个本地视频文件或一个 B站链接 → ② 看它给出的时间戳转录与关键帧
- **期望**：有转录、有场景切分、有画面理解
- **实际**：
- **判定**：待测
- **证据**：截图
