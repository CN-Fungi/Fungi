# 人3 · 边界与异常（单机，不需要第二台机器）

- 执行人：<你的 GitHub 用户名>
- 机器：<机器名> / 网段 <192.168.x.x>
- 版本：Fungi v0.6.1 打包版；解压路径 <纯英文路径>
- 日期：2026-09-__
- 本份文件同时是 **场③（三机与异常）的记录员** 用表，见 `场3-三机与异常.md`

这一份是"挑毛病"的活：正常路径别人已经跑过了，你专门走错路、走极端、读文案。

## 登记表（结果填这里，逐条改成 通过 / 失败 / 不可测）

| 编号 | 用例 | 判定 | Issue |
|---|---|---|---|
| 3-01 | 不配 key | 待测 | |
| 3-02 | 单机模式（`--web` / 一句话） | 待测 | |
| 3-03 | 错 token / 空 token | 待测 | |
| 3-04 | 帮助页 vs 真实界面 | 待测 | |
| 3-05 | 权限滑块 UI 与落盘 | 待测 | |
| 3-06 | 对面离线时的提示 | 待测 | |
| 3-07 | 长时间挂托盘 | 待测 | |
| 3-08 | 界面文案与错别字清单 | 待测 | |

---

### 3-01 不配 key

- **前置**：把 `config.json` 备份后清掉（或改成一个空 key）
- **步骤**：① 启动应用 → ② 直接试一次对话
- **期望**：提示怎么配 key，不崩、不静默、不卡旋转
- **实际**：
- **判定**：待测
- **证据**：截图
- **收尾**：测完把 `config.json` 恢复

### 3-02 单机模式

- **前置**：无
- **步骤**：① `python -m fungi "一句话查询"` → ② `python -m fungi --web`（不开房间）
- **期望**：都能用；没有房间时状态说清楚，不假装在房间里
- **实际**：
- **判定**：待测
- **证据**：终端截图

### 3-03 错 token / 空 token

- **前置**：朋友已经开好房间
- **步骤**：① 填一个错 token 加入 → ② 什么都不填直接加入 → ③ 填对
- **期望**：前两次被拒且提示可读（说清是 token 不对，而不是"连接失败"）；第三次成功
- **实际**：
- **判定**：待测
- **证据**：三次的截图

### 3-04 帮助页 vs 真实界面

- **前置**：无
- **步骤**：把 `帮助` 页每一条对着实际界面走一遍
- **期望**：逐条对得上；**对不上的一条一条列出来**（文案过期也算缺陷）
- **实际**：
- **判定**：待测
- **证据**：帮助页截图 + 不一致处的对照清单

### 3-05 权限滑块 UI 与落盘

- **前置**：有一个好友（另一台机器开着）
- **步骤**：① 进好友视图找顶部权限滑块 → ② 拨到「允许」→ ③ 拨回「询问」→ ④ 看 `~/.fungi/consent_rules.json`
- **期望**：两档状态可辨认；拨完立即生效；文件里真写进去了
- **实际**：
- **判定**：待测
- **证据**：滑块截图 + json 内容截图

### 3-06 对面离线时的提示

- **前置**：有好友在列表里
- **步骤**：① 让对面直接关掉应用 → ② 你继续发消息 → ③ 再发一个文件
- **期望**：超时后给出可读提示，而不是无限转圈、也不是静默消失
- **实际**：
- **判定**：待测
- **证据**：截图

### 3-07 长时间挂托盘

- **前置**：在房间里
- **步骤**：关窗转到托盘，正常用 1 小时以上
- **期望**：内存不持续涨、托盘图标不消失、期间收发正常
- **实际**：
- **判定**：待测
- **证据**：任务管理器前后两次截图

### 3-08 界面文案与错别字清单

- **前置**：无
- **步骤**：把六个页面（发起房间 / 加入房间 / 手机端 / 信使 / 设置 / 帮助）里的中文文案通读一遍
- **期望**：交一份清单——哪一页、哪一句、问题是什么（错别字 / 歧义 / 与行为不符 / 中英混排不一致）
- **实际**：
- **判定**：待测
- **证据**：清单本身（每条附截图更佳）
